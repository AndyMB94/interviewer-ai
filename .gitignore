# Python
__pycache__/
*.py[cod]
.venv/
db.sqlite3

# Node
node_modules/
dist/
build/

# Env secrets (cada servicio commitea su .env.example, nunca su .env)
.env

# Audio de prueba (grabaciones personales, no se comparten)
*.wav
*.mp3
*.m4a

# Audio generado en runtime (TTS)
backend/media/

# Estáticos generados por collectstatic (Infra Fase 4) — se regeneran en cada arranque del contenedor
backend/staticfiles/

# OS / editor
.DS_Store

# Archivos de prueba manual con curl
cookies.txt
